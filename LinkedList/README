The SingLinked java file runs off of the Main Java file. I did not code anything in the Main file, but I coded most of the methods in the SingLinked.
You can open the SingLinked file and check the comments to see where my lab partner and I made changes to the code
